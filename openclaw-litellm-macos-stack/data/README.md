This directory is intentionally kept out of version control except for this note.

Runtime state such as Postgres files, OpenClaw credentials, paired devices, Telegram allowlists,
session history, and local workspaces will be recreated here when the stack runs.
